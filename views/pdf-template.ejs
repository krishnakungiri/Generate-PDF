<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>PDF Viewer</title>
  </head>
  <body>
    <% if (pdfData) { %>
    <!-- Embed the PDF data as base64 -->
    <embed
      src="data:application/pdf;base64,<%= pdfData %>"
      type="application/pdf"
      width="100%"
      height="600px"
    />
    <% } else { %>
    <p>No PDF data available</p>
    <% } %>
  </body>
</html>
